#include <iostream>
#include "shop.h"
using namespace std;

// The main function is the entry point of the program.
// It creates an instance of the Shop class and calls its main method,
// which handles the user interactions and controls the overall flow of the program.
int main(){
    Shop shop; // Create an instance of the Shop class
    shop.main(); // Call the main method of the Shop class
    return 0;
}
